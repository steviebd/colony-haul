name: import-p19-stretch
on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/import-p19-stretch.yml'
permissions:
  contents: write
jobs:
  import:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Import Pass 19 files
        env:
          TARBALL: https://d.uguu.se/eWwJvUeH.tgz
        run: |
          set -euo pipefail
          curl -fsSL "$TARBALL" -o /tmp/p19.tgz
          tar xzf /tmp/p19.tgz
          git config user.name "cursoragent"
          git config user.email "cursoragent@cursor.com"
          git add \
            Assets/_ColonyHaul/Scripts/Core/GameSim.cs \
            Assets/_ColonyHaul/Scripts/Boot/SliceHud.cs \
            Assets/_ColonyHaul/Scripts/Boot/SliceJuice.cs \
            Assets/_ColonyHaul/Scripts/Boot/VerticalSliceBootstrap.cs \
            README.md
          if git diff --cached --quiet; then
            echo "no file changes"
          else
            git commit -m "Unity slice: CREW STRETCH names the idle pad when crew is short."
          fi
          rm -f .github/workflows/import-p19-stretch.yml
          git add .github/workflows/import-p19-stretch.yml
          if git diff --cached --quiet; then
            echo "workflow already gone"
          else
            git commit -m "Drop Pass 19 import workflow."
          fi
          git push
