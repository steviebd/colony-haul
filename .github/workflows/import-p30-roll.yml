name: import-p30-roll
on:
  push:
    paths:
      - '.github/workflows/import-p30-roll.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  import:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Import Pass 30 HAUL ROLL slice
        run: |
          set -euo pipefail
          git config user.name "colony-haul-import"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          curl -fsSL -o /tmp/p30.tar.gz "https://h.uguu.se/emnVkvLr.tar.gz"
          tar -tzf /tmp/p30.tar.gz
          tar -xzf /tmp/p30.tar.gz -C .
          git add README.md Assets/_ColonyHaul/Scripts/Core/GameSim.cs Assets/_ColonyHaul/Scripts/Boot/VerticalSliceBootstrap.cs
          if git diff --cached --quiet; then
            echo "no slice changes"
          else
            git commit -m "Unity slice: HAUL ROLL chips cargo ETA on the other loaded carts."
            git push
          fi
          git pull --rebase origin main
          rm -f .github/workflows/import-p30-roll.yml
          git add -A .github/workflows
          if git diff --cached --quiet; then
            echo "workflow already gone"
          else
            git commit -m "Drop Pass 30 import workflow."
            git push
          fi
