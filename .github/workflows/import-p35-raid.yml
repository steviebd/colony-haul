name: import-p35-raid
on:
  push:
    paths:
      - '.github/workflows/import-p35-raid.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  import:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Import Pass 35 RAID n slice
        run: |
          set -euo pipefail
          git config user.name "colony-haul-import"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          curl -fsSL -o /tmp/p35.tar.gz "https://h.uguu.se/RdnzNRud.tgz"
          tar -tzf /tmp/p35.tar.gz
          tar -xzf /tmp/p35.tar.gz -C .
          git add README.md Assets/_ColonyHaul/Scripts/Core/GameSim.cs Assets/_ColonyHaul/Scripts/Boot/VerticalSliceBootstrap.cs
          if git diff --cached --quiet; then
            echo "no slice changes"
          else
            git commit -m "Unity slice: RAID n live raider count on the Hub."
            git push
          fi
          git pull --rebase origin main
          rm -f .github/workflows/import-p35-raid.yml
          git add -A .github/workflows
          if git diff --cached --quiet; then
            echo "workflow already gone"
          else
            git commit -m "Drop Pass 35 import workflow."
            git push
          fi
