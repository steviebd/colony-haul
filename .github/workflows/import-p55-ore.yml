name: import-p55-ore
on:
  push:
    paths:
      - '.github/workflows/import-p55-ore.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  import:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Import Pass 55 ORE n slice
        run: |
          set -euo pipefail
          git config user.name "colony-haul-import"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          curl -fsSL -o /tmp/p55.tar.gz "https://h.uguu.se/iwRaDLHy.tar.gz"
          tar -tzf /tmp/p55.tar.gz
          tar -xzf /tmp/p55.tar.gz -C .
          git add README.md Assets/_ColonyHaul/Scripts/Boot/VerticalSliceBootstrap.cs Assets/_ColonyHaul/Scripts/Core/GameSim.cs
          if git diff --cached --quiet; then
            echo "no slice changes"
          else
            git commit -m "Unity slice: ORE n colony spend sits under LARDER on the Hub."
            git push
          fi
          git pull --rebase origin main
          rm -f .github/workflows/import-p55-ore.yml
          git add -A .github/workflows
          if git diff --cached --quiet; then
            echo "workflow already gone"
          else
            git commit -m "Drop Pass 55 import workflow."
            git push
          fi
