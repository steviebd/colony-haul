name: import-p17-openchoke
on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/import-p17-openchoke.yml'
permissions:
  contents: write
jobs:
  import:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Import Pass 17 files
        env:
          TARBALL: https://d.uguu.se/wyAUrNAK.tgz
        run: |
          set -euo pipefail
          curl -fsSL "$TARBALL" -o /tmp/p17.tgz
          tar xzf /tmp/p17.tgz
          git config user.name "cursoragent"
          git config user.email "cursoragent@cursor.com"
          git add \
            Assets/_ColonyHaul/Scripts/Core/GameSim.cs \
            Assets/_ColonyHaul/Scripts/Boot/SliceHud.cs \
            Assets/_ColonyHaul/Scripts/Boot/SliceJuice.cs \
            Assets/_ColonyHaul/Scripts/Boot/VerticalSliceBootstrap.cs \
            README.md
          if git diff --cached --quiet; then
            echo "no file changes"
          else
            git commit -m "Unity slice: OPEN CHOKE names the unarmed hottest lane."
          fi
          rm -f .github/workflows/import-p17-openchoke.yml
          git add .github/workflows/import-p17-openchoke.yml
          if git diff --cached --quiet; then
            echo "workflow already gone"
          else
            git commit -m "Drop Pass 17 import workflow."
          fi
          git push
