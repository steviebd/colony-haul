name: import-p41-cut
on:
  push:
    paths:
      - '.github/workflows/import-p41-cut.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  import:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Import Pass 41 CUT n slice
        run: |
          set -euo pipefail
          git config user.name "colony-haul-import"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          curl -fsSL -o /tmp/p41.tar.gz "https://n.uguu.se/EXolwTkN.tgz"
          tar -tzf /tmp/p41.tar.gz
          tar -xzf /tmp/p41.tar.gz -C .
          git add README.md Assets/_ColonyHaul/Scripts/Core/GameSim.cs Assets/_ColonyHaul/Scripts/Boot/VerticalSliceBootstrap.cs
          if git diff --cached --quiet; then
            echo "no slice changes"
          else
            git commit -m "Unity slice: CUT n live snapped rails sit left of DOWN."
            git push
          fi
          git pull --rebase origin main
          rm -f .github/workflows/import-p41-cut.yml
          git add -A .github/workflows
          if git diff --cached --quiet; then
            echo "workflow already gone"
          else
            git commit -m "Drop Pass 41 import workflow."
            git push
          fi
