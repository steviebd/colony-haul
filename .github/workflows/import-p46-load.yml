name: import-p46-load
on:
  push:
    paths:
      - '.github/workflows/import-p46-load.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  import:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Import Pass 46 LOAD n slice
        run: |
          set -euo pipefail
          git config user.name "colony-haul-import"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          curl -fsSL -o /tmp/p46.tar.gz "https://d.uguu.se/XNfOFYyL.tar.gz"
          tar -tzf /tmp/p46.tar.gz
          tar -xzf /tmp/p46.tar.gz -C .
          git add README.md Assets/_ColonyHaul/Scripts/Core/GameSim.cs Assets/_ColonyHaul/Scripts/Boot/VerticalSliceBootstrap.cs
          if git diff --cached --quiet; then
            echo "no slice changes"
          else
            git commit -m "Unity slice: LOAD n loaded haulers sit under WAVE CLOCK."
            git push
          fi
          git pull --rebase origin main
          rm -f .github/workflows/import-p46-load.yml
          git add -A .github/workflows
          if git diff --cached --quiet; then
            echo "workflow already gone"
          else
            git commit -m "Drop Pass 46 import workflow."
            git push
          fi
