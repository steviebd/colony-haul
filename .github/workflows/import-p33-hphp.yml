name: import-p33-hphp
on:
  push:
    paths:
      - '.github/workflows/import-p33-hphp.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  import:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Import Pass 33 HUB HP slice
        run: |
          set -euo pipefail
          git config user.name "colony-haul-import"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          curl -fsSL -o /tmp/p33.tar.gz "https://n.uguu.se/WiCpSpul.tar.gz"
          tar -tzf /tmp/p33.tar.gz
          tar -xzf /tmp/p33.tar.gz -C .
          git add README.md Assets/_ColonyHaul/Scripts/Core/GameSim.cs Assets/_ColonyHaul/Scripts/Boot/VerticalSliceBootstrap.cs
          if git diff --cached --quiet; then
            echo "no slice changes"
          else
            git commit -m "Unity slice: HUB HP number sits beside the world bar."
            git push
          fi
          git pull --rebase origin main
          rm -f .github/workflows/import-p33-hphp.yml
          git add -A .github/workflows
          if git diff --cached --quiet; then
            echo "workflow already gone"
          else
            git commit -m "Drop Pass 33 import workflow."
            git push
          fi
