name: import-p16-l2ready
on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/import-p16-l2ready.yml'
permissions:
  contents: write
jobs:
  import:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Import Pass 16 files
        env:
          TARBALL: https://n.uguu.se/DjKRGDjS.tgz
        run: |
          set -euo pipefail
          curl -fsSL "$TARBALL" -o /tmp/p16.tgz
          tar xzf /tmp/p16.tgz
          git config user.name "cursoragent"
          git config user.email "cursoragent@cursor.com"
          git add \
            Assets/_ColonyHaul/Scripts/Core/GameSim.cs \
            Assets/_ColonyHaul/Scripts/Boot/SliceHud.cs \
            Assets/_ColonyHaul/Scripts/Boot/SliceJuice.cs \
            Assets/_ColonyHaul/Scripts/Boot/VerticalSliceBootstrap.cs \
            README.md
          if git diff --cached --quiet; then
            echo "no file changes"
          else
            git commit -m "Unity slice: L2 READY names the Hub spend that unlocks Splash."
          fi
          rm -f .github/workflows/import-p16-l2ready.yml
          git add .github/workflows/import-p16-l2ready.yml
          if git diff --cached --quiet; then
            echo "workflow already gone"
          else
            git commit -m "Drop Pass 16 import workflow."
          fi
          git push
