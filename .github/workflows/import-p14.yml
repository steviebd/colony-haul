name: import-p14
on:
  push:
    paths:
      - '.github/workflows/import-p14.yml'
  workflow_dispatch:
jobs:
  import:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - name: Fetch and unpack
        run: |
          curl -fsSL -o /tmp/p14.tgz "https://n.uguu.se/Prslflji.tgz"
          tar -tzf /tmp/p14.tgz
          tar -xzf /tmp/p14.tgz
      - name: Commit import and drop workflow
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add Assets/_ColonyHaul/Scripts/Core/GameSim.cs \
            Assets/_ColonyHaul/Scripts/Boot/SliceHud.cs \
            Assets/_ColonyHaul/Scripts/Boot/SliceJuice.cs \
            Assets/_ColonyHaul/Scripts/Boot/VerticalSliceBootstrap.cs \
            README.md
          git commit -m "Unity slice: SIT stock names the piled pad haulers skipped." || true
          rm -f .github/workflows/import-p14.yml
          git add .github/workflows/import-p14.yml
          git commit -m "chore: drop one-shot import Pass 14" || true
          git push
