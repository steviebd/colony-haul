name: import-p39-last
on:
  push:
    paths:
      - '.github/workflows/import-p39-last.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  import:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Import Pass 39 LAST slice
        run: |
          set -euo pipefail
          git config user.name "colony-haul-import"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          curl -fsSL -o /tmp/p39.tar.gz "https://n.uguu.se/oiBZJczB.tgz"
          tar -tzf /tmp/p39.tar.gz
          tar -xzf /tmp/p39.tar.gz -C .
          git add README.md Assets/_ColonyHaul/Scripts/Core/GameSim.cs Assets/_ColonyHaul/Scripts/Boot/VerticalSliceBootstrap.cs
          if git diff --cached --quiet; then
            echo "no slice changes"
          else
            git commit -m "Unity slice: LAST chips the final live raider."
            git push
          fi
          git pull --rebase origin main
          rm -f .github/workflows/import-p39-last.yml
          git add -A .github/workflows
          if git diff --cached --quiet; then
            echo "workflow already gone"
          else
            git commit -m "Drop Pass 39 import workflow."
            git push
          fi
