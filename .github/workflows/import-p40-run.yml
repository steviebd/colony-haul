name: import-p40-run
on:
  push:
    paths:
      - '.github/workflows/import-p40-run.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  import:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Import Pass 40 RUN n slice
        run: |
          set -euo pipefail
          git config user.name "colony-haul-import"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          curl -fsSL -o /tmp/p40.tar.gz "https://d.uguu.se/huihTgLm.tgz"
          tar -tzf /tmp/p40.tar.gz
          tar -xzf /tmp/p40.tar.gz -C .
          git add README.md Assets/_ColonyHaul/Scripts/Core/GameSim.cs Assets/_ColonyHaul/Scripts/Boot/VerticalSliceBootstrap.cs
          if git diff --cached --quiet; then
            echo "no slice changes"
          else
            git commit -m "Unity slice: RUN n live runners sit beside DOWN."
            git push
          fi
          git pull --rebase origin main
          rm -f .github/workflows/import-p40-run.yml
          git add -A .github/workflows
          if git diff --cached --quiet; then
            echo "workflow already gone"
          else
            git commit -m "Drop Pass 40 import workflow."
            git push
          fi
