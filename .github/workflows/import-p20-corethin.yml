name: import-p20-corethin
on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/import-p20-corethin.yml'
permissions:
  contents: write
jobs:
  import:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Import Pass 20 files
        env:
          TARBALL: https://h.uguu.se/pQvjPfdq.tgz
        run: |
          set -euo pipefail
          curl -fsSL "$TARBALL" -o /tmp/p20.tgz
          tar xzf /tmp/p20.tgz
          git config user.name "cursoragent"
          git config user.email "cursoragent@cursor.com"
          git add \
            Assets/_ColonyHaul/Scripts/Core/GameSim.cs \
            Assets/_ColonyHaul/Scripts/Boot/SliceHud.cs \
            Assets/_ColonyHaul/Scripts/Boot/SliceJuice.cs \
            Assets/_ColonyHaul/Scripts/Boot/VerticalSliceBootstrap.cs \
            README.md
          if git diff --cached --quiet; then
            echo "no file changes"
          else
            git commit -m "Unity slice: CORE THIN names the BRACE haul when Hub HP cracks."
          fi
          rm -f .github/workflows/import-p20-corethin.yml
          git add .github/workflows/import-p20-corethin.yml
          if git diff --cached --quiet; then
            echo "workflow already gone"
          else
            git commit -m "Drop Pass 20 import workflow."
          fi
          git push
