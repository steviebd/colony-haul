name: import-p56-shrug
on:
  push:
    paths:
      - '.github/workflows/import-p56-shrug.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  import:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Import Pass 56 BRACE SHRUG slice
        run: |
          set -euo pipefail
          git config user.name "colony-haul-import"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          curl -fsSL -o /tmp/p56.tar.gz "https://n.uguu.se/wDHfahJO.tar.gz"
          tar -tzf /tmp/p56.tar.gz
          tar -xzf /tmp/p56.tar.gz -C .
          git add README.md Assets/_ColonyHaul/Scripts/Boot/VerticalSliceBootstrap.cs Assets/_ColonyHaul/Scripts/Boot/SliceJuice.cs Assets/_ColonyHaul/Scripts/Core/GameSim.cs
          if git diff --cached --quiet; then
            echo "no slice changes"
          else
            git commit -m "Unity slice: BRACE SHRUG — shield feel over Hub counters."
            git push
          fi
          git pull --rebase origin main
          rm -f .github/workflows/import-p56-shrug.yml
          git add -A .github/workflows
          if git diff --cached --quiet; then
            echo "workflow already gone"
          else
            git commit -m "Drop Pass 56 import workflow."
            git push
          fi
