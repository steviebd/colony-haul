name: import-p47-off
on:
  push:
    paths:
      - '.github/workflows/import-p47-off.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  import:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Import Pass 47 OFF n slice
        run: |
          set -euo pipefail
          git config user.name "colony-haul-import"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          curl -fsSL -o /tmp/p47.tar.gz "https://n.uguu.se/uaoBdwUv.tar.gz"
          tar -tzf /tmp/p47.tar.gz
          tar -xzf /tmp/p47.tar.gz -C .
          git add README.md Assets/_ColonyHaul/Scripts/Core/GameSim.cs Assets/_ColonyHaul/Scripts/Boot/VerticalSliceBootstrap.cs
          if git diff --cached --quiet; then
            echo "no slice changes"
          else
            git commit -m "Unity slice: OFF n unrouted pads sit left of RAID."
            git push
          fi
          git pull --rebase origin main
          rm -f .github/workflows/import-p47-off.yml
          git add -A .github/workflows
          if git diff --cached --quiet; then
            echo "workflow already gone"
          else
            git commit -m "Drop Pass 47 import workflow."
            git push
          fi
